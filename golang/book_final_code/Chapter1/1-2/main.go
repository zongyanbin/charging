package main

/**
25个关键字

包相关(2)：		package import

声明相关(9)：	var	const(定义常量) type(定义类型)
			struct(结构体) interface(接口) func(函数)
			channel(通道) map(字典) go
循环声明(2)：	for range

条件判断相关(5)：	if else switch case select

中断或返回(6)：break default continue或return goto fallthrough

延迟执行(1): defer
-----------------------------------------------------------------------------------------------------------
常量：true false iota nil
函数：make len cap new append copy close delete complex real imag panic recover

格式化代码ctrl alt +l
*/
func main() {
	//	变量
	//	var age int = 1
	//	age := 18
}
